# Accessibility Checklist

| Check | Status | Notes |
|-------|--------|-------|
| Page has semantic landmarks (`header`, `main`, `nav`, `footer`) | To do | |
| Headings are ordered logically (no skipping levels) | To do | |
| All interactive elements are keyboard accessible | To do | |
| Focus states are visible on all interactive elements | To do | |
| Buttons use `<button>` elements | To do | |
| Links use `<a>` elements | To do | |
| Form labels are present and associated with inputs | To do | |
| Error messages are programmatically associated with their inputs | To do | |
| Colour contrast meets WCAG AA (4.5:1 for text, 3:1 for large text) | To do | |
| Images have useful `alt` text or are marked as decorative (`alt=""`) | To do | |
| Modals trap focus when open and return focus on close | To do | |
| Page is usable at 200% zoom | To do | |
